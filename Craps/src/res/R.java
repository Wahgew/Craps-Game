package res;

import java.awt.*;
/**
 * Resource class providing color constants for UI elements.
 * @author Peter Madin
 * @version 0.0.1 December 7, 2023
 */
public class R {
    /**
     * Inner static class containing color constants for various UI elements.
     */
    public static class Colors {
        /** Background color for UI elements. */
        public static final Color BACKGROUND = new Color(53, 101, 77);

        /** Color for outlines or borders in UI elements. */
        public static final Color OUTLINES = Color.WHITE;

        /** Text label color for UI elements. */
        public static final Color TEXT_LABEL = Color.WHITE;

        /** Button color for UI elements. */
        public static final Color BUTTON = Color.BLACK;

        /** Color for displaying numbers in UI elements. */
        public static final Color NUMBERS = new Color(255, 182, 18);

        /** Header color for UI elements. */
        public static final Color HEADER = new Color(133, 41, 40);
    }
}